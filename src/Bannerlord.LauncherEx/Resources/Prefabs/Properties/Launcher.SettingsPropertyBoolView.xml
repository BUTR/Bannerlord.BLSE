<Prefab>
  <Window>
    <Widget HeightSizePolicy="StretchToParent" WidthSizePolicy="Fixed" SuggestedWidth="150" VerticalAlignment="Center" MarginRight="15" IsVisible="@IsBoolVisible">
      <Children>
        <Launcher.ToggleButton WidthSizePolicy="Fixed" HeightSizePolicy="Fixed" SuggestedWidth="22" SuggestedHeight="22" VerticalAlignment="Center" HorizontalAlignment="Center" IsSelected="@BoolValue" />
      </Children>
    </Widget>
  </Window>
</Prefab>